# Contributing

Contributions are welcome.

Before opening a pull request, run:

```bash
pip install -e ".[dev]"
pytest
```

Please do not commit datasets, generated outputs, caches, model checkpoints, plots, or local environment files.
